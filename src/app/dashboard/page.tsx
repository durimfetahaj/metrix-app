import { FC } from "react";

const page: FC = () => {
  return (
    <div className="h-screen flex items-center justify-center bg-brand-background">
      <div>
        <p>Dashboard</p>
      </div>
    </div>
  );
};

export default page;
